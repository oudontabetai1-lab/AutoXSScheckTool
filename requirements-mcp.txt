# Requirements for the OOB email MCP server (wscan/oob_email_mcp.py) and the
# MFA solver MCP client (wscan/mfa.py). Install in addition to requirements.txt:
#   pip install -r requirements-mcp.txt
#
# OOB email sink: configure WSCAN_OOB_* env (see docs/oob_email_ja.md) and run:
#   python -m wscan.oob_email_mcp
#
# MFA (2FA) automation calls *external* MCP servers as a stdio client. These are
# installed separately (not Python deps of this repo):
#   - TOTP : https://github.com/gosusnkr/mcp-totp-authenticator  (Node)
#   - Email: https://github.com/ai-zerolab/mcp-email-server       (uvx/pip)
# Configure via WSCAN_MFA_* env (see README).

mcp>=1.0.0
